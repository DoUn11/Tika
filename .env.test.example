# 로컬 PostgreSQL 테스트 전용 접속 정보.
# 이 파일을 .env.test 로 복사한 뒤 <password> 를 실제 값으로 바꾼다.
# .env.test 는 커밋하지 않는다.

POSTGRES_URL=postgresql://tika:<password>@localhost:5432/tika_test
POSTGRES_URL_NON_POOLING=postgresql://tika:<password>@localhost:5432/tika_test
